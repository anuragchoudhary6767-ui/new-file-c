#include <stdio.h>
#include <string.h>
int main()
{   
    char str[]="aiouabvs";
    int count;
    int cCount=0;
    int cCount=0;
    for( int i<0;i<ln;i++)
    
        char ch==str[i];
        {
         if (str[i]=='a'||str[i]=='e'||str[i]=='i'||str[i]=='o'||str[i]=='u')
         {
            vCount++;
         }
         else
         {
            cCount++;
         }
        }
        printf( "%d %d",vCount,cCount)
};